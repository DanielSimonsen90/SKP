import ComponentCollection from '../Collection/ComponentCollection';
import NamedNodeArray from '../Collection/NamedNodeArray';
import DOMTokenArray from '../Collection/DOMTokenArray';

export default interface IComponent {
    preRender(): this;
    render(): string;
    rendered(): this;
    propUpdate(currentValue: any, updatedValue: any): this;

    id: string;
    props: {};
    children: ComponentCollection;
    attributes: NamedNodeArray;
    classes: DOMTokenArray;
}